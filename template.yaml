import json

from app.mock_data import build_mock_response


def lambda_handler(event, context):
    response = build_mock_response().model_dump()
    return {
        "statusCode": 200,
        "headers": {"Content-Type": "application/json"},
        "body": json.dumps(response),
    }
